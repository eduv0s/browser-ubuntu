{
  "name": "browser-ubuntu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:netlify": "npm run prepare:image && vite build",
    "prepare:image": "node scripts/prepare-image.mjs",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": { "jsdom": "^22.1.0", "vite": "^5.4.0", "vitest": "^1.6.0" }
}
